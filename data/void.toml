description = """
`std::convert::Infallible` (stable since Rust 1.34) covers every use of the \
`Void` type. The crate's `Result` extension helpers have no stable equivalent \
yet (`Result::into_ok` / `into_err` are unstable).
"""
url = "https://doc.rust-lang.org/std/convert/enum.Infallible.html"
