description = """
Most of `once_cell` is now in `std`: `OnceCell`/`OnceLock` (Rust 1.70) and \
`LazyCell`/`LazyLock` (Rust 1.80). The `race` module and fallible \
`get_or_try_init` have no stable `std` equivalent yet.
"""
url = "https://blog.rust-lang.org/2024/07/25/Rust-1.80.0/"
